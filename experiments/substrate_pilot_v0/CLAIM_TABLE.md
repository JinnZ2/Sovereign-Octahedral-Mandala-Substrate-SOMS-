# CLAIM TABLE — substrate pilot v0.1 (+V11, v0.2 round 3 + authority factorial)

Status vocabulary: BUILT (selftest asserts it) | PARTIAL (built, coverage declared) |
UNRUN (instrument exists, no run) | BLOCKED (reason). Nulls N-1..N-4 have their own rows.

| id | claim | status | evidence | note |
|---|---|---|---|---|
| Q-1 | refusal path identified | ANSWERED | BUILD_NOTE_v0.1.md | two paths; physical-ledger side effect fixed and injects rerun |
| V1 | every failure record carries LOCUS + SITE; physical_damage rejected off damaged sites; enum defined (learning added, site unknown added) | BUILT | selftest `V1_failure_locus_schema` incl. `test_enum_is_defined` | 9 failure record types route through one validator; `LOCUS_DEFINITIONS` in ledger.py; the two-grader result (2/12 exact, 6/12 disjoint) is why the definitions exist; ledger-emitted FLT failures recoded custody -> learning; Maria row 5 retained as the table's custody with an editor note for the second grader |
| V2 | OIG-20-76 coded fixture, 13 rows, header, tally per LOCUS, empty second-grader slots | BUILT | `fixtures/maria_locus.jsonl`, `locus_tally.py`, selftest `V2_maria_fixture` | tally 10/2/1 is single-grader; `tally_citable: false` |
| V3 | class + axis per load; "critical" alone refused; custody axis may not drop | BUILT | selftest `V3_regime_class`; I-13 | release gate also holds a classless load |
| V4 | event declaration reclasses from precomputed table with zero manual steps; water -> R2 min | BUILT | selftest `V4_context_severity`; I-11 | `manual_steps` recorded on the EVENT record (N-3 watch) |
| V5 / FT-16 | signed handover at every boundary; presence without custody = unsigned transfer | BUILT | selftest `FT16_named_custodian`; I-12 | grace 1 h before a drop reads as a transfer |
| V6 / FT-17 | declared silence interval on R2/R3; escalation within one interval; latency metric | BUILT | selftest `FT17_heartbeat`; I-9 | detection depends on the sweep running each interval |
| V7 / FT-18 | competing claim routes to resolver; cellular-only resolver refused | BUILT | selftest `FT18_contested_custody`; I-10 | custody never moves on a claim, only on resolution |
| V8 | assignments persist post-event; missing + no WAIVED -> flagged at activation | BUILT | selftest `V8_retention` | protein_g left unassigned in the replay on purpose |
| V9 | I-9..I-13 run; results with shall and target columns | BUILT | `runs/exercise_v0_1.md` | FT-01 and FT-13 fail target by inject construction |
| V10 | Katrina intake, LOCUS-coded, separate fixture; GAO 2018 third fixture | PARTIAL, SOURCE TEXT LOADED | `fixtures/sources.json` (K1 K2 K3 M1 M2 M3, LINK), `fixtures/katrina_locus.jsonl`, `fixtures/gao2018_locus.jsonl` | titles/IDs/dates/URLs verified 2026-09-10 (order §3a); K3 pp.1-43, K2 fn 2, M2 highlights, M3 one sentence fetched (fixtures/katrina_passages.jsonl); K1 logistics chapter and K3 pp.66-77, 128-134 not read; recalled mechanism rows retained and marked; verbatim passages carried with locus null for graders; not citable until graded |
| V10b | report-type split: review/self-review code heavier on damage than audits; retention K3 -> OIG-10-101 -> M1 | NOT A TEST (N=1 review row, N=1 self-review row) | `locus_tally.report_type_split`; `doc_type`, `row_kind`, `recurred_in_M1` fields | verbatim stated causes present with locus null: M1 A1/A2 (audit) and C4 (management-comment rebuttal, self-review specimen), K3-1/K3-2/K3-5 (audit), K2-1 (review, measurand declaration), M2-1 (review), M3-1 (self-review); grader template `fixtures/stated_causes_grader_template.jsonl`; retention coded by the executor on the 10 recalled Katrina mechanism rows: yes 6, not_assessable 4 (K3-sourced rows alone: yes 1, not_assessable 1); NULL untested |
| V1-fix | SITE is a record fact supplied with the finding, never graded | BUILT | `site_record` on every fixture row; `check_locus` reads it; selftest `test_site_is_a_record_fact` | graded round-1 sites kept as history, marked deprecated; GPT graded-site vs record mismatches computed (>= 5) |
| V2-3way | three graders, three families | BUILT (computed) | `locus_tally.three_way`; selftest | any regime component 12/13/13; pure damage 1/0/0; pairwise top level 9, 8, 11 of 13; damage unanimous-NO on 8 rows, unanimous-YES on none; row 5 three answers |
| V2-round2 | cold re-grade under V1 definitions with SITE supplied, 14 rows | BUILT (computed) | `locus_tally.round2`; selftest | exact 11/14, overlap 2, disjoint 1 (row 9), mean Jaccard 0.857, kappa over label sets computed from the rows 0.664 (order: 0.66); custody in both gradings on 8+ rows; physical damage 2 vs 1 |
| V2c | custody split state / responsibility; cold re-grade on OIG verbatim sentences, 17 rows | BUILT (computed) | `fixtures/maria_locus_round3_results.jsonl` (per row: variant P0/P1, grader, family, locus, verified, date loaded 2026-09-11; grading date not supplied; prompt files P0/P1 referenced by name, not supplied); `locus_tally.round3_block`; selftest `V2c_round3_results` | P0 graders gpt, deepseek, kimi; gemini P0 REFUSED. Computed: pairwise exact full G-D 14 / G-K 11 / D-K 13, collapsed 15 / 13 / 15; unanimous 11/17 full, 13/17 collapsed; any regime component 15 / 16 / 15; pure physical_damage 2 / 1 / 2, unanimous only on row 13; custody.state 24 of 28 custody labels on 9 all-custody rows, unanimous cs on 1, 3, 8a, 8b, 10, 12, C2; rule violation gpt row 9 (physical_damage at undamaged record site) flagged and kept; stability r2->r3 collapsed gpt 14/14, deepseek 13/14 (row 4) |
| V2c-4fam | four families with gemini P1 | BUILT (computed) | same file, `four_family_unanimous_collapsed` | 13/17 collapsed, the same row set as three families; open rows computed over four families: 2, 9, 11, C3 (the order lists 2, 9, C3; row 11 is open because kimi P0 coded physical_damage where the others coded custody) |
| V2c-gemini | gemini probe | ANSWERED (partial) | `gemini_probe` in the block | P0 (FEMA, Crowley, Puerto Rico named) refused; P1 (names removed) answered: the names were the trigger; company vs agency not yet split (T-d) |
| V2c-name | name effect P0 vs P1, same grader | BUILT (computed) | `name_effect` in the block | deepseek 17/17 identical; gpt 16/17, the one flip is row 9 physical_damage -> regime.custody.state; kimi UNVERIFIED |
| V2c-kimi | kimi P1 | UNVERIFIED, EXCLUDED | `verified: false` lines in the results file; selftest `test_kimi_P1_is_excluded_not_counted` | byte-identical to gemini P1 on all 17 rows: paste duplication or convergence cannot be told apart; excluded from every figure until re-copied by the operator (T-c) |
| V2c-cite | citable statement | CITABLE (computed basis) | maria header `tally_citable.round3_sub_axis_collapsed: true`; selftest reproduces every figure | sub-axis citable with custody collapsed (13/17 unanimous, three families; 13/17 with four); custody split citable as "state >> responsibility" (24 of 28); pure damage 1-2 of 17 on an audit of an undamaged-site supply chain |
| V2c-pending | T-a label pull (physical_damage -> event_damage, row 9, gpt + gemini cold); T-b place prior (row 9 P0, gpt, 3 cold sessions); T-c kimi re-copy; T-d gemini P2 (names kept, contractor rows removed) | TEMPLATES BUILT, UNRUN | `fixtures/pending/small_tests.jsonl`; selftest `test_pending_small_tests_templates` | T-a and T-b both bear on row 9 (the only rule violation and the only name flip); T-d removes rows 2, C1, C3 (they name the carrier) and keeps row 4 |
| V2d | authority x source 2x2: does the coding of the same sentence move with the agent named and the source frame? place = "the island"; rows 2, 9, 11, C1, C3 + anchors 1, 13; 4 cells x 3 graders (gpt, deepseek, gemini; kimi once T-c verifies) | INSTRUMENT BUILT, PROMPTS EMITTED, UNRUN | `v2d_authority/` (stimuli.json, v2d.py, prompts/authority_A{high,low}_S{high,low}.txt, runs/constructed.jsonl); selftest `V2d_authority_instrument`, `V2d_prompt_files` | rows are the OIG-20-76 verbatim sentences with the substitutions declared in `v2d.SUBS` (FEMA -> agent, Crowley -> the carrier, Puerto Rico -> the island, Jacksonville -> the mainland port); selftest asserts each file differs from its agent neighbour by exactly the agent string and from its source neighbour by exactly the prefix line, and that each row differs from `maria_verbatim.jsonl` only by declared substitutions. Measures: external share, internal-failure share, market share, hedge rate. Predictions, either direction a finding: DEFERENCE (high agent -> more external), SYMPATHY (low agent -> more external), CREDIBILITY (high source -> coding follows framing; low source -> more hedging/refusal), NULL (no cell beyond 1/7: coding tracks mechanism, not authority; reported as such). Anchors must hold or the cell is reported broken. Confound declared: low-source plausibility on 3 rows. Grammar artifacts of direct substitution are kept, not smoothed |
| CAVEAT | audit-instrument bias toward custody.state | OPEN | maria header `caveats`; `tally_citable.sub_axis: true` collapsed; "state >> responsibility" | an IG audit reads the record trail, so custody.state may dominate because that is what an audit can see; the enum needs a run on a non-audit source before the dominance generalizes; the citable statements above are audit-instrument statements |
| V2-sites | SITE as record fact, now sourced | BUILT | `site_record`, `site_basis`, `oig_page` on every Maria row from the OIG text | the row-9 site was supplied as damaged in error in round 2 ([site check] confirmed); the only round-2 disjoint row is the one whose supplied site was wrong |
| V11 | CLIMATE on every spec row, load and trigger table; missing -> stable + flagged; event flips climate via V4 table; targets carry their climate; claim valid only inside its climate; field-fix check at every contact under variable | BUILT | selftest `V11_climate`; `spec_rows.json`; replay row V11 | FT-05's stable drill target now reads NOT VALID (climate) in the variable run instead of FAIL; unattended drops are the field-fix-missing contacts |
| V10c | within-document: stated-cause sentences vs finding sentences of the same document, same enum | COMPUTED for M1; K3 NOT EVALUABLE | `locus_tally.v10c`; `fixtures/stated_causes_grader_template.jsonl` (P0 graders filled, K2-1 not coded); selftest `test_v10c_M1_computed_A1_unanimous` | M1: stated-cause external share 0.667 (2 of 3) per grader vs findings external share gpt 0.118, deepseek 0.059, kimi 0.176; A1 unanimous mixed(physical_damage, regime.urgency), the audit's own sentence naming both; K3 findings K3-3, K3-4, K3-6 ungraded. Reads: the summary attributes to the event, the findings code the record trail |
| V10b | report-type split | NOT A TEST (feasibility only) | stated-cause rows now graded by the P0 graders | N=1 review, N=1 self-review; the instrument computes but the denominators are not there |
| N-1 | second grader moves >= 3 Maria rows to physical_damage | NOT TRIGGERED | `locus_tally.agreement`: rows moved to physical_damage by grader 2 = 0; pure physical_damage 1 -> 0 | the second grader moved rows AWAY from damage; the damage/regime separation holds under both codings; the regime sub-axis does not |
| N-2 | Katrina tally majority physical_damage at damaged sites | NOT TRIGGERED (provisional) | katrina fixture: 7 regime / 2 mixed / 1 physical_damage of 10 | provisional: partial coverage, unverified sources; re-evaluate after extraction from the texts |
| N-3 | I-11 reclass requires a manual step | NOT TRIGGERED | EVENT record `manual_steps: 0` | reclass is one call on a table declared pre-event |
| N-4 | heartbeat escalation fires but no resolver acts within the interval | NOT TRIGGERED in this run | FT-18 row: resolver acted at 4 h | the row splits shall/target so a silent resolver shows as shall held, target failed |

Out of scope (unchanged): REC beyond depth 1, HF/mesh transport, Phase 3 Choice token, Phase 4 beyond the single settlement gate.
